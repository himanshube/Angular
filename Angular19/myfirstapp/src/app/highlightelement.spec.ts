import { Highlightelement } from './highlightelement';

describe('Highlightelement', () => {
  it('should create an instance', () => {
    const directive = new Highlightelement();
    expect(directive).toBeTruthy();
  });
});
